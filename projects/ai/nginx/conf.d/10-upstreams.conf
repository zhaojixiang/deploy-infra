# Rewritten by scripts/blue-green-deploy.sh — do not edit by hand during deploy
upstream frontend_active {
    server frontend_blue:80;
    keepalive 32;
}

upstream backend_active {
    server backend_blue:3000;
    keepalive 32;
}
